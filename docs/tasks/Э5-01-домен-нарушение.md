# Э5-01. Доменная модель «Нарушение» + подразделения + классификатор + риск

| | |
|---|---|
| Этап | Э5 — модули фазы Ф1 (фундамент под Архив/Риски/Мониторинг/Дашборд) |
| Статус | 🔄 В работе (шаг 1 — модель данных) |
| Требования ТЗ | §5.2.5, §5.3.2.8; Приложение §2 (риск), §4 (нарушение). Схема `inspector` |
| Документы | [ДОК-04 схема данных](../04_Схема_данных.md), [Методика расчёта риска](../reference/методика-расчёта-риска.md) |
| Зависит от | Э3-01 (InspectorDbContext), связано с СКИД (source_doc_ref по значению) |

## Цель
Собрать **структурный объект «Нарушение»** и опоры под него (иерархия подразделений, 2-уровневый классификатор), плюс **детерминированную формулу риска**. На этом стоят модули Архив, Риски, Мониторинг, Дашборд. Всё — в схеме `inspector` (профиль); ИИ здесь НЕ считает, только тексты рекомендаций (позже).

## Модель (Приложение §4)
**`inspector.violation`** (заносится человеком):
| Поле | Тип/значения |
|---|---|
| division_id | ссылка на `inspector.division` (иерархия ТУ→РО) |
| category_id | ссылка на классификатор (2 уровня) |
| severity | 4 уровня: низкая/средняя/высокая/критическая |
| detected_at | дата выявления |
| source_doc_ref | RegNumber документа СКИД (по значению) |
| source_assignment_ref | id поручения СКИД (по значению, опц.) |
| remediation_status | устранено / на контроле / частично / просрочено |
| cause | текст (может быть ИИ-черновиком) |
| recommendation | текст (может быть ИИ-черновиком) |
| reference_doc_id | id справки-проверки (по значению) |
| is_recurring | производное (по category+division во времени) |

**`inspector.division`** — иерархия ТУ→РО (`parent_id`), сопоставление с плоским справочником СКИД по коду (§4.2).
**Классификатор видов** — 2 уровня (сфера → вид), расширяемый (ведёт администратор). Стартовый набор из прототипа (агентурная работа, АОД, документооборот, планирование, исполнение поручений, взаимодействие подразделений, работа по направлениям) — уточняемо, не блокирует.

## Формула риска (Приложение §2, детерминированная — код, не ИИ)
```
RiskScore(подразделение, период) =
    w1·Σ severity_weight(нарушение) + w2·RepeatCount + w3·OverdueRemediationCount + w4·TrendDelta
severity_weight: низкая=1, средняя=2, высокая=4, критическая=8
веса по умолчанию: w1=1.0, w2=1.5, w3=2.0, w4=1.0
уровни (пороги T1<T2<T3 калибруются на Э1): <T1 низкий, [T1,T2) средний, [T2,T3) высокий, ≥T3 критический
дашборд: 3 цвета (зелёный=низкий, жёлтый=средний, красный=высокий+критический)
```

## Шаги
- ✅ **Шаг 1 — модель данных:** сущности `Violation`/`Division`/`ViolationCategory` (все `: AuditableEntity`) + enum `ViolationSeverity`/`RemediationStatus`, EF-конфиги (FK внутри схемы, Restrict; индексы под аналитику), DbSet в `InspectorDbContext`, миграция `AddViolationDomain` (3 таблицы схемы `inspector`). Сборка 0/0; юнит 90/90.
- ✅ **Шаг 2 — формула риска:** `RiskScoreCalculator` (Domain/Risk) — детерминированный балл по формуле Приложения §2 (`w1·Σseverity_weight + w2·повторы + w3·просрочки + w4·тренд`; веса тяжести 1/2/4/8), веса/пороги конфигом (`RiskWeights`/`RiskThresholds`, стартовые калибруются Э1), балл → уровень (`RiskLevel`) → 3 цвета (`RiskColor`, высокий+критич.=красный). Зарегистрирован в DI. Тесты `RiskScoreCalculatorTests` (формула/веса/пороги/воспроизводимость). Сборка 0/0; юнит 97/97.
- ✅ **Шаг 3 — учёт и светофор (2026-08-24):** порты домена (`IViolationStore`/`IViolationCategoryStore`/`IRiskDataSource` + `ViolationContracts`), хранилища в Data: реестр (повторность — производная, отбор по сфере включает её виды, счёт до среза страницы), классификатор (2 уровня enforced, идемпотентный сид 7 стартовых сфер только на пустом), источник сигналов риска (открытые тяжести / повторы «тот же вид в том же подразделении» / просрочки / тренд к предыдущему периоду равной длины). Сценарии Application c гардами и аудитом: учёт ведут Инспектор/Руководитель/Администратор (`ViolationGuard`, fail-closed), классификатор — Администратор (`AdministrationRule`). UI: `/violations` (реестр с фильтрами + карточка занесения/правки; категория — сфера целиком или уточняющий вид, решение заказчика 2026-08-24), `/admin/violation-categories` (дерево как справочник подразделений), **живой `/dashboard`** — демо-данные сняты, счётчики периода (30/90/180/365 дней), светофор по подразделениям (балл/уровень/цвет), лента последних нарушений. Тесты: юнит 299/299 (гарды), интеграция 101/101 — в т.ч. ручной балл 19 = формула §2 на настоящем Postgres (`RiskDataSourceTests`, `ViolationRegistryTests`).
- ✅ **Шаг 4 — «Риски и контроль» и «Мониторинг» на живых данных (2026-08-24, ветка `feature/live-risk-monitoring`):** `IRiskDataSource` расширен `GetDivisionRisksAsync` (разбор риска: сигналы формулы по отдельности, болевые сферы «Сфера — N», последняя непустая рекомендация) и `GetRemediationAsync` (карточки устранения — неустранённые первыми, срез 100, счётчики статусов по подразделениям). Запросы `GetDivisionRisksQuery`/`GetRemediationQuery` (View-аудит, период 7–366 дней). Страницы `/risks` и `/monitoring` переведены с демо на живой учёт (селектор периода, разбор балла в деталях; прогресс устранения — детерминированная свёртка статусов: устранено=100/частично=50/на контроле=25/просрочено=0). Демо-наборы Risks/Monitoring и остатки старого дашборда удалены из `InspectorDemoData` (остались Генератор/Методики/Анализ/правила контроля), компонент `DivisionsTable` удалён. Тесты: `RiskDataSourceTests` — разбор и мониторинг сходятся с ручным расчётом.
- ⏳ **Шаг 5 — связи по значению** с документами/поручениями: реквизиты (`source_doc_ref`/`source_assignment_ref`/`reference_doc_ref`) заносятся строками в карточке; разрешение «справки-проверки» в карточку документа сделано в Архиве (Э5-03, порт `IDocumentLookup`); автоподстановка при занесении — по потребности.

## Решения по модели (шаг 1)
- Ссылки на СКИД (`SourceDocRef`/`SourceAssignmentRef`/`ReferenceDocRef`) — **строкой по значению**, без FK через границу схем (ТО-инф-06).
- FK подразделения/вида — `DeleteBehavior.Restrict` (не удалить подразделение/вид, пока по ним есть история нарушений).
- `is_recurring` (повторность) — **производное**, в сущности НЕ хранится; считается в аналитике риска (шаг 2) по вид+подразделение во времени.
- `ViolationSeverity` — чистая категория (Low/Medium/High/Critical); весовые 1/2/4/8 — в расчётчике риска (шаг 2), не в перечне.

## Критерии приёмки
- Нарушение создаётся с обязательными подразделением, видом и тяжестью; связь с СКИД — по значению (без FK через границу схем).
- На фиксированном наборе уровень риска **воспроизводим** и совпадает с формулой (модульный тест, §5.2.5.1).
