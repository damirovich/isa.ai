# Э4-12. Гигиена документации — синхронизация статусов и ссылок с кодом (P2)

| | |
|---|---|
| Этап | Э4 — гигиена (P2) |
| Статус | ⏳ Запланировано |
| Требования ТЗ | ТД (актуальность документации) |
| Источник | Аудит 2026-06-26 (раздел discrepancies — расхождения док↔код) |
| Зависит от | — |

## Проблема
Код везде ≥ заявленного, но документация местами рассинхронена (косметика, не дефекты кода).

## Что сделать
- ⏳ **Э1-03** — поднять статус до ✅: контракты (`IRetriever`/`AccessContext`/грунтовка/ingestion/docx/аудит) фактически реализованы в `Abstractions` и покрыты тестами.
- ⏳ Снять устаревшие внутренние ⏳-маркеры в разделе «Что сделать» у **Э3-01**, **Э3-04** (заголовок и «Результат» = ✅, код = ✅).
- ⏳ **Э3-03** — переписать раздел «Осталось»: авто-запись действий уже есть (Search/Generate/Export пишут вызовами; сквозное поведение — отдельная Э4-11).
- ⏳ Починить битые ссылки: **Э4-03** (`GenerateReferenceHandler.cs` → хендлер вложенный в команду), **Э3-07** (`IngestionTests` → `IngestionFailClosedTests`/`SimpleTextChunkerTests`).
- ⏳ **docs/04_Схема_данных.md** — убрать утверждения «состояние кода — Э0, DbContext/миграций нет, проекты-пустышки» (устарело; Э3 реализован).
- ⏳ **ADR-0015** — выровнять поля `HarvestedDocument` (нет `FetchedAt`/`ContentType` в реальном record).
- ⏳ Устаревшие снимки счётчиков тестов в доках — обновить (или заменить формулировкой «актуальное число — в CI»).

## Критерии приёмки
- Заявленные статусы и ссылки в `docs/tasks/*` и `docs/04_*` соответствуют коду.

## Примечание
Счётчики тестов — point-in-time-метрики; чтобы не гонять их вручную, можно перестать фиксировать абсолютные числа в доках.
