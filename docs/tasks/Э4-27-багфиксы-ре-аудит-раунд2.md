# Э4-27. Исправление багов по повторному ре-аудиту ядра (раунд 2)

| | |
|---|---|
| Этап | Э4 — hardening |
| Статус | 🔄 В работе — по одному пункту |
| Источник | Повторный сквозной ре-аудит ядра 2026-07-23 (после [Э4-25](Э4-25-багфиксы-ре-аудит.md)). Регрессий 0 (все 7 правок раунда 1 держатся); найдено 7 новых подтверждённых расхождений |

Повторный проход подтвердил, что правки раунда 1 не сломались, и нашёл новые пробелы — в т.ч. **грани**
двух пунктов, которые в раунде 1 были помечены закрытыми (ТБ-010 — только AllowedHosts, ТБ-030 — только
задвоение/субъект). Закрываем по одному, с тестами.

## Чек-лист (по приоритету)

| # | Требование | Суть | Оценка | Статус |
|---|---|---|---|---|
| 1 | ~~**ТБ-010 (cookie)**~~ | ✅ Закрыто 2026-07-23 — cookie аутентификации `SecurePolicy` → `Always` (была `SameAsRequest`: за TLS-прокси с HTTP-хопом cookie уходила без Secure). Настройка вынесена в `AuthCookieConfiguration` + тест `AuthCookieConfigurationTests`. | мал., ядро | ✅ |
| 2 | ~~**ТБ-030 (аудит сбоя генерации)**~~ | ✅ Закрыто 2026-07-23 — `GroundedGenerator` при сбое/отмене модели пишет запись ПОПЫТКИ (гриф=max фрагментов, субъект, id фрагментов) через `CancellationToken.None`, затем пробрасывает. Блокирующий путь — try/catch, стриминг — try/finally (yield с catch нельзя). Тест `GroundedGeneratorTests` (модель падает → аудит попытки + исключение). | мал., ядро | ✅ |
| 3 | **ТО-мат-03 (docx)** | `DocxTextExtractor` склеивает абзацы одинарным `\n`, а чанкеры считают границей `\n\n` → весь .docx = один абзац → слепой рез посреди предложения. | мал., ядро | ⏳ |
| 4 | **ТНД-002 (потеря шарда)** | `BundleImporter` при отсутствии файла-шарда делает молчаливый `continue` — целый шард пропадает без счётчика/лога/отчёта; оператор видит «успех». | мал., ядро | ⏳ |
| 5 | **ТБ-013 (канал СКИД)** | Строка подключения к СКИД без `SSL Mode=Require` (секрет + данные по чужому хосту незашифрованно). `Username=postgres`/`skid_Dev` — deployment-настройка оператора. | мал., ядро | ⏳ |
| 6 | **ТО-инф-03 (метаданные)** | `IngestionRequest.Metadata` (идентификатор нормы, статус редакции) прокидывается, но `IngestionPort` его не читает, у `DocumentEntity` нет поля → словарь молча теряется. | ср., профиль | ⏳ |
| 7 | **ТБ-014 (step-up)** | Нет повторной аутентификации под привилегированные действия (удаление/загрузка/генерация/экспорт). Idle-таймаут и принудительный выход — есть. | ср., feature | ⏳ |

## Отклонённые находки (для истории — НЕ баги)

- Вся подсистема фоновых задач (4 находки: гонка Enqueue, MarkRunning вне try, потеря Completed при
  рестарте, усечение Error>8000) — **PREMATURE**: очередь построена и подключена как hosted-сервис, но
  НИ ОДИН прод-код не вызывает `EnqueueAsync` (проверено grep'ом) → дефекты недостижимы. Закрыть до
  появления первого продюсера очереди.
- ТН-003 (устойчивость стриминга) — PREMATURE: `GenerateStreamingAsync` не подключён к UI/команде.
- ТО-мат-03 (перекрытие/семантика чанков) — PREMATURE: overlap осознанно отложен «поверх ITextChunker»
  (Э3-07), базовый чанкер — задекларированная заглушка. *(Отдельно от п.3 — там баг разделителя docx.)*
- ТБ-033 — FALSE_POSITIVE: маркировка грифа проставляется на всех живых путях (`ClassificationMarking`
  обязателен, `ClassificationMarking.For` всегда непустой).
- ТБ-016 — PREMATURE: аудит выдачи/отзыва допуска — часть админ-UI, который не построен (допуски —
  SQL-скриптом DBA).
