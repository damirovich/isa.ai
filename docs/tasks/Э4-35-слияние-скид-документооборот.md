# Э4-35. Слияние СКИД: пакет модулей документооборота (`docflow`)

| | |
|---|---|
| Этап | Э4/Э5 — функциональные модули |
| Статус | 🔄 Этапы 0–1 ✅; этап 2.1 ✅ (документное ядро: 7 таблиц, агрегат §4.3, инвариант §3.1). Следующий — 2.2 (комментарии/уведомления/настройки) либо этап 3 (сценарии) (ветка `feature/skid-docflow-merge`) |
| Решение | [ADR-0017](../06_ADR/ADR-0017-skid-modul-dokumentooborota-ne-profil.md) — СКИД = пакет модулей, НЕ второй профиль; размещение — `src/modules/docflow/` |
| Требования ТЗ | ТС-002/003/004/006/007 (ядро, профиль, реестр модулей); ТО-инф-01 (схемы); ТБ-012/020/021 (допуск, fail-closed); ТБ-030 (аудит); ТСТ-001 (лицензии) |
| Исходное состояние | СКИД в эксплуатацию **не введена, боевых данных нет** (подтверждено 2026-08-04) — переноса данных не требуется, схема поднимается с нуля |
| Источники | Код `repos/skid` (~38 тыс. строк) + **ТЗ СКИД** (ДСП, ред. 26.06.2026): «АИС контроля — Систематизация контрольно-инспекторской деятельности», заказчик — **Главная инспекция при Председателе ГКНБ КР** (тот же, что и у «ИнспекторAI») |
| Оценка | **7–10 недель** до калибровочного захода; уточняется после этапа 1 |

## Замысел

Влить СКИД (контроль исполнения документов, ~38 тыс. строк) в ISC.AI так, чтобы инспектор
получил **одно приложение с одним входом**, где «Документы» и «Поручения» стоят рядом с «Чат-ассистентом»
и «Генератором», а ИИ работал по **живым** документам и поручениям, а не по отдельной копии.

Форма слияния (обоснование — в ADR-0017): СКИД **не** становится вторым профилем (профиль в ISC.AI ровно
один на развёртывание), а становится переиспользуемым пакетом модулей, который профиль включает в свой
реестр.

## Целевая структура

```
src/modules/docflow/
  ISC.AI.Modules.DocFlow/              манифест: дескрипторы модулей + AddDocFlow(...)
  ISC.AI.Modules.DocFlow.Domain/       сущности и перечисления (из SKID.Domain)
  ISC.AI.Modules.DocFlow.Application/  сценарии Mediator, валидаторы, DTO
  ISC.AI.Modules.DocFlow.Data/         DocFlowDbContext (схема docflow) + миграции
  ISC.AI.Modules.DocFlow.UI/           RCL: страницы модулей
```

Зависимости (ядро не трогаем): `модуль → Abstractions` (и `Persistence` — только `*.Data`);
`профиль → модуль`; **ядро → НЕ ссылается на модуль** (арх-тест ТС-009 расширить).

Подключение — одной строкой в манифесте профиля: `InspectorProfile.Modules` дополняется секцией меню
«Документооборот», `RegisterServices`/`RegisterDataContexts` вызывают `AddDocFlow(...)`.

## Как ложатся RAG и грунтовка (частый вопрос)

**Заблуждение:** «документы лягут в схему `docflow` ⇒ RAG должен искать по схеме `docflow`».
**На самом деле:** RAG не ищет по доменным таблицам вообще. Он ищет по **индексу**, а индекс всегда в `core`.

Это два разных хранилища с разными задачами:

| | Схема `docflow` — **домен** | Схема `core` — **индекс** |
|---|---|---|
| Что лежит | Документ как объект работы: рег. №, исполнитель, срок, статус, поручения, файл | Текст, порезанный на фрагменты, + векторы |
| Кто читает | Страницы «Документы» / «Поручения» | Только `IRetriever` (RAG) |
| Зачем | Вести документооборот | Искать по смыслу |

Мост между ними — существующий конвейер загрузки (`IIngestionPort`):

```
docflow.document              core.document              core.chunk + вектор
рег.№ 12-345, срок,    ──→    текст, гриф,        ──→    фрагменты, гриф,
исполнитель, файл             подразделение,             подразделение,
                              StorageUri                 IsCurrent
```

Далее всё работает без изменений: `IRetriever` фильтрует по допуску **прямо в SQL** (гриф ≤ допуска И
подразделение ∈ разрешённых И `IsCurrent`) — гриф и подразделение денормализованы на сам чанк
(`ChunkEntity`) именно для того, чтобы не джойнить через границы схем; затем грунтовка разрешает ссылаться
ТОЛЬКО на выданные фрагменты и проверяет каждую ссылку валидатором (ТБ-040/041). **Схема-источник в этом
не участвует** — ядро не знает и не должно знать, откуда пришёл текст.

**Прецедент уже в коде:** ровно так живут НПА — домен норм в схеме `inspector`, индекс в `core`, связь
таблицей-мостиком `inspector.norm_document_link` (слабая ссылка по `document_id`, **без FK через границу
схем** — ТО-инф-06). Документооборот получает такой же мостик `docflow.document_index_link`, чтобы ответ
чата мог сослаться на конкретное поручение и открыть его.

Отсюда следует, что работа не в «поиске по docflow», а в **подаче** документов СКИД в существующий
конвейер — это отдельный этап 7 ниже.

## Что уточнило ТЗ СКИД (прочитано 2026-08-05)

**Заказчик тот же** — Главная инспекция при Председателе ГКНБ КР. Это снимает последние сомнения в форме
слияния: одни и те же люди, один продукт (ADR-0017).

**1. Доступ в СКИД — построчный, он НЕ выражается решёткой «гриф × подразделение».** Это главная находка,
она расширяет этап 6:

| Роль | Что видит по ТЗ (разд. 2.1) |
|---|---|
| Администратор | **Доступ к документам ПОЛНОСТЬЮ ЗАКРЫТ**; только пользователи, справочники, журнал действий |
| Руководитель | Все документы системы; единственный, кто ставит «Снято с контроля» |
| Инспектор | **Только свои документы** (где он назначен инспектором) — включая отчёты и дашборд |
| Исполнитель | **Только назначения, где он ответственный** |

Наш фильтр ядра — «гриф ≤ допуска И подразделение ∈ разрешённых». Правила выше **ортогональны** ему
(владение/назначение, а не гриф). Ложатся они на подключаемую сужающую политику `IAccessPolicy`
([ADR-0014](../06_ADR/ADR-0014-rezhim-podklyuchaemaya-politika-dostupa.md): «floor ядра + сужающая
политика»), а не на простое сопоставление «роль → политика», как было написано в этапе 6 изначально.
Отдельно отмечено: «администратор не видит документов» — правило **на снятие** доступа у самой
привилегированной роли; его нельзя реализовать как «больше прав = больше видно».

**2. Отчёты — состав известен, риск №1 сузился** (разд. 6): **6 отчётов** (по лицу/инспектору, по
подразделению, по срокам, по просроченным, по типам документов, по периоду) × **3 формата** (PDF, Excel,
Word). Плюс п. 8.3 — **персонализация печати**: колонтитул с именем пользователя, датой и временем печати
(режимно значимо — прослеживаемость печати ДСП, смыкается с нашим аудитом ТБ-030).

**3. Подразделения — конфликт справочников конкретизировался** (вопрос 3): в СКИД справочник **плоский**,
ключ — **Guid**; в профиле «Инспектор» — **иерархический** `Division` (ТУ→РО) с `Code`, ключ — **int**, и
он уже сопоставляется со СКИД по значению кода. Плюс архитектурная тонкость: `docflow` — переиспользуемый
модуль, он **не может зависеть от профиля**, поэтому «просто взять `Division` профиля» нельзя.

**4. Требования, которые надо сохранить при переносе:**
- **Двуязычие рус/кырг** (п. 7.1) — требование ТЗ, а не опция; в «ИнспекторAI» аналога пока нет.
- **Файлы только для просмотра в интерфейсе, без скачивания** (п. 3.3) — согласуется с режимом ДСП.
- Версионирование файла при замене (п. 3.3); история статусов, продлений и переназначений (п. 4.8).
- Уведомления за 7 дней и в день срока, **только в интерфейсе, без email** (разд. 5) — согласуется с air-gap.
- Журнал действий доступен **только Администратору** (разд. 10) — у нас журнал строже (неизменяемый,
  хеш-цепочка), правило видимости переносится.
- Производительность: ≥20 одновременных пользователей, отклик ≤3 с (п. 8.2).

**5. Несовпадения типов ключей при переносе:** СКИД — `Guid` (пользователи, подразделения, документы),
ядро ISC.AI — `int` (`AppUserEntity.Id`, `DivisionId` в `core`). Решается на этапе 2; данных нет, поэтому
выбор свободный, но он влияет на связку с `AccessContext.AllowedDivisions` (там `int`).

**6. Грифа в ТЗ СКИД нет вообще** — подтверждает вопрос 2: правило назначения грифа определяет режим
«ИнспекторAI», из СКИД его взять неоткуда. При этом сам документ ТЗ имеет пометку **ДСП**, то есть
предметная область режимная по факту.

## Этапы

### Этап 0. Подготовка — ✅ ГОТОВО
- [x] ADR-0017 — форма слияния зафиксирована.
- [x] Скелет пакета: 5 проектов + ссылки + `ISC.AI.slnx`. Правок CPM НЕ потребовалось — все пакеты
      (Mediator.Abstractions, FluentValidation, EF Core, Npgsql, MudBlazor…) уже в
      `Directory.Packages.props` от профиля.
- [x] Манифест `DocFlowModule` (реестр страниц + `RegisterServices`/`RegisterDataContexts`),
      `DocFlowDbContext` со схемой `docflow` и своей историей миграций, шов DI собран.
- [x] Профиль подключил пакет: `InspectorProfile` включает `.. DocFlowModule.Modules` и вызывает
      регистрацию модуля — **шов композиции проверен сборкой**, а не «на бумаге».
- [x] Архитектурный тест расширен тремя правилами: ядро не ссылается на модуль; **модуль не зависит от
      профиля и хоста** (ключевое для переиспользуемости); модуль зависит внутрь (`*.Data` → Persistence).
      Правила проверены **мутацией**: временная ссылка модуль→профиль роняет тест (1 из 188), после отката —
      188/188. Сборка 0 ошибок.
- [x] **Реестр лицензионных замен** — [docs/reference/скид-реестр-лицензионных-замен.md](../reference/скид-реестр-лицензионных-замен.md):
      MediatR — 55 файлов Application (50 обработчиков) + 13 тестовых; FluentAssertions — 23 файла;
      QuestPDF — только `PdfReportGenerator` (+DI), **штамп печати уже на PDFsharp** — риск этапа 5 меньше
      ожидаемого.

> **Именование.** Проекты названы `ISC.AI.Modules.*` (мн. число), а не `ISC.AI.Module.*`: анализатор даёт
> CA1716 — `Module` совпадает с зарезервированным словом языка. Переименовано сразу, пока проекты пустые.

### Этап 1. Калибровочный заход — ✅ ГОТОВО (2026-08-05, за один рабочий заход)
Перенесён **справочник типов документов** (ТЗ §3.1) сквозняком: Domain → Data → Application → UI → меню.
- **Domain:** `DocumentGroup` (Хранение/Исполнение) + `DocumentType : AuditableEntity` (int-ключ по
  конвенции ядра вместо Guid СКИД — данных нет, выбор свободный) + порт `IDocumentTypeStore`
  (слоистость как у профиля: Application на Data не ссылается).
- **Data:** конфигурация (`docflow.document_type`, unique-имя, CHECK по группе), `DocumentTypeStore`,
  дизайн-фабрика, миграция `InitialDocFlow` (создана; к БД применяет заказчик).
- **Application — образец переноса MediatR → Mediator** (`DocumentTypeScenarios.cs`, отличия
  задокументированы в шапке файла): 1 запрос + 3 команды; ручной `IAuditService` СКИД → сквозной
  `AuditBehavior` по `IAuditableRequest` (в ядро добавлено значение `AuditAction.Modify` — справочники);
  `[RequireRole]` → политика модуля (роли — этап 6); валидаторы FluentValidation остались как были.
- **UI:** страница `/docflow/types` (таблица, добавление, переименование, деактивация, смена группы) —
  без компонентов профиля (модуль от профиля не зависит); пункт меню в секции «Документооборот».
- **Инвариант §3.1** «группа не меняется при наличии документов» — сформулирован в порте; проверка
  наличия документов — обязательство этапа 2 (сущности «Документ» пока нет, помечено в
  `DocumentTypeStore.ChangeGroupAsync`).
- **Проверено:** сборка 0 ошибок; юнит **194/194** (+6); интеграционный тест на реальном Postgres
  (Testcontainers) — миграция применяется, весь CRUD-цикл зелёный; превью хоста — секция и страница
  в меню, рендер работает, отсутствие схемы в dev-БД даёт дружелюбную ошибку через
  `ExceptionHandlingBehavior` (graceful, не белый экран).
- **Вывод по темпу:** одна фича-группа СКИД со ВСЕЙ одноразовой обвязкой пакета — один заход; в
  Application СКИД ~12 сопоставимых фич-групп. Диапазон **7–10 недель подтверждается**, при таком темпе —
  ближе к нижней границе (риски этапов 5–6 не пересматривались).

### Этап 2. Domain + Data — ~1 неделя. Разбит на 2.1 / 2.2

#### 2.1 Документное ядро — ✅ ГОТОВО (2026-08-05)
Миграция `DocumentCore`: 7 таблиц (`document`, `document_file`, `document_attachment`,
`document_assignment`, `assignment_status_history`, `status_history_file`, `deadline_extension`,
`deadline_extension_file` — 8-я включая файлы продлений) + 5 перечислений.
- Перенос из СКИД с заменами конвенций: ключи Guid → **int**; `DirectionFlag` → `DocumentDirection`
  (CA1711), колонка осталась `direction_flag`; ссылки на пользователей — **слабые int** на
  `core.app_user` без FK через границу схем (ТО-инф-06); FK внутри схемы docflow — настоящие,
  каскады как в СКИД; **xmin** сохранён на документе и назначении (системная колонка, в миграции
  не материализуется); CHECK-ограничения и индексы дашборда перенесены.
- **Гриф и подразделение на документе — NOT NULL с первого дня** (ADR-0017 п.5): `classification` +
  `division_id` (+ индекс `division_id, classification` под решётку).
- **`AggregatedStatusCalculator` (§4.3) перенесён дословно** в Domain/Services как чистая функция;
  8 юнит-тестов фиксируют каждую строку правила приоритетов, включая унаследованную особенность
  (смесь Исполнено+Снято → Зарегистрирован — ТЗ смесь не определяет, поведение СКИД сохранено 1:1).
- **Инвариант §3.1 «группа не меняется при наличии документов» — теперь РЕАЛЬНЫЙ**: обязательство
  этапа 1 выполнено, `ChangeGroupAsync` проверяет документы типа; интеграционный тест на отказ
  `HasDocuments` — на настоящем Postgres.
- Проверено: сборка 0 ошибок / 0 предупреждений в новом коде; юнит **202/202** (+8);
  интеграционные docflow **2/2**.

#### 2.2 Остаток домена — впереди
- Комментарии с упоминаниями (`document_comment`, `document_comment_file`, `document_comment_mention`,
  enum `CommentType`), уведомления (`notification`, enum `NotificationType`), системные настройки
  (`system_setting`), `UiLanguage`/`ReportFormat` — по мере надобности сценариев.
- `users`/`departments` СКИД — таблицы НЕ переносятся как есть: пользователи = `core.app_user`
  (решение по вопросу 4), подразделения — см. вопрос 3 ниже. `audit_logs` СКИД не переносится —
  аудит идёт в неизменяемый журнал ядра (этап 4).
- Матрица переходов статусов (§4.5) — вместе со сценариями смены статуса (этап 3), там же роли «кто
  может» (этап 6).

### Этап 3. Application — 1–1.5 недели
- **~116 обработчиков MediatR → Mediator (martinothamar)**: `IRequest`/`IRequestHandler` → наши контракты,
  `Task` → `ValueTask`, генератор — только в хосте.
- `AuthorizationBehavior` СКИД (атрибут `[RequireRole]`) заменяется нашими сквозными поведениями
  (аудит, авторизация, проверка грунтовки).
- FluentValidation остаётся (одна и та же библиотека, Apache 2.0) — правки минимальны.

### Этап 4. Инфраструктура — ~1 неделя
- Хранилище файлов, конвертер DOCX→PDF (LibreOffice, внешний процесс — учесть в деплое контура),
  уведомления (SignalR-хаб), системные настройки, фоновая проверка дедлайнов.
- **Аудит СКИД сливается с нашим неизменяемым журналом** (хеш-цепочка, append-only) — не два журнала.

### Этап 5. Отчёты — ~1 неделя (риск сузился)
- **QuestPDF → PDFsharp / OpenXml** (лицензия, ТСТ-001). Не переименование: у QuestPDF декларативный
  layout, у PDFsharp низкоуровневый API — отчёты переписываются.
- **Состав известен из ТЗ (разд. 6)** — 6 отчётов × 3 формата: по ответственному лицу/инспектору, по
  подразделению, по срокам, по просроченным, по типам документов, по периоду; форматы PDF, Excel, Word.
  Excel (ClosedXML) и Word (OpenXml) лицензионно чистые — **переписывается только ветка PDF**.
- **Персонализация печати** (п. 8.3 ТЗ): колонтитул с именем пользователя, датой и временем печати.
  Режимно значимо — прослеживаемость печати ДСП; связать с аудитом (ТБ-030), а не делать «просто текстом».

### Этап 6. Доступ, роли и идентичность — 1–1.5 недели ⚠️ РИСК (проектное решение, не код)

**Доступ и роли:**
- Каждый документ/поручение получает **гриф и подразделение**; выдача — через существующий фильтр
  (fail-closed, ТБ-020/021). Понятия грифа в СКИД нет — правило назначения при регистрации определяет
  режим (вопрос 2). Унаследованных документов нет, поэтому «правило для старых записей» не требуется.
- Подразделения: `departments` СКИД и подразделения ISC.AI сводятся к одному справочнику (вопрос 3).
- Роли СКИД (Администратор/Руководитель/Инспектор/Исполнитель) → именованные политики
  `IModule.RequiredPolicy` (закрывает отложенный пункт
  [ADR-0016](../06_ADR/ADR-0016-skid-istochnik-identichnosti-dopusk-lokalno.md)).
- ⚠️ **Построчные правила ТЗ (разд. 2.1) — сверх решётки, через `IAccessPolicy`** (ADR-0014, сужающая
  политика поверх floor ядра). Их четыре, и они про владение/назначение, а не про гриф:
  «Администратор НЕ видит документов вообще» (снятие доступа у привилегированной роли — нельзя выразить
  как «больше прав»); «Руководитель — все»; «Инспектор — только свои документы»; «Исполнитель — только
  свои назначения». Это отдельная проектная работа, а не сопоставление ролей политикам.

**Идентичность — реестр пользователей переезжает к нам (решено 2026-08-05, вопрос 4):**
- Таблицы `users`/`departments` переносятся вместе с остальной схемой; администрирование учёток
  (создание, временный пароль, сброс, блокировка) наследуется из СКИД как экраны модуля.
- Порт `IExternalIdentityProvider` в `Abstractions` **СОХРАНЯЕТСЯ** — это шов под будущий SSO; под ним
  появляется **локальная** реализация вместо адаптера чужой БД. Проверка Argon2id (`SkidPasswordHash`,
  Sodium.Core) переезжает как есть.
- **Выводятся из решения:** проект `src/integrations/ISC.AI.Identity.Skid` (read-only контекст чужой БД),
  строка `ConnectionStrings:Skid`, секрет `Database:Passwords:Skid` и организационное требование
  read-only учётки `iscai_ro`. Минус внешняя БД, минус секрет, минус сетевая зависимость в контуре.
- Вход, cookie, security stamp и ревалидация сессий (`ISC.AI.Web/Security`) **не меняются** — меняется
  только реализация под портом.
- Собственный вход СКИД (`/auth/login`, своя cookie, своя ревалидация) выбрасывается — вход один, наш.
- ⚠️ **Порядок обязателен:** адаптер выводится ТОЛЬКО после того, как локальная идентичность заработала,
  иначе система остаётся без аутентификации.
- Инвариант сохраняется: **допуск НЕ выводится из учётки** (default-deny, ТБ-012) даже после того, как
  и идентичность, и допуск стали локальными.

### Этап 7. Индексация документов в корпус (RAG) — 0.5–1 неделя 🎯 РАДИ ЭТОГО ВСЁ ЗАТЕВАЕТСЯ
Без этого этапа слияние даёт только «одно меню»; ИИ по документам инспекции работать не начнёт.
- **Мостик** `docflow.document_index_link`: слабая ссылка `документ docflow ↔ core.document` по значению,
  **без FK через границу схем** (ТО-инф-06) — по образцу `inspector.norm_document_link`.
- **Подача в конвейер:** документ/вложение docflow → извлечение текста (DOCX/PDF; сканы — OCR, Э4-29) →
  `IIngestionPort` с **обязательными** грифом и подразделением (правило этапа 6; не задано ⇒ отказ, ТБ-024).
- **Триггер и переиндексация:** при регистрации, добавлении вложения и изменении. При замене версии старые
  фрагменты гасятся (`IsCurrent=false` / `SupersededByDocumentId` — механизм в ядре уже есть, Э4-14).
- **Асинхронно** — через очередь фоновых задач ядра (`AddCoreBackgroundTasks`): регистрация документа в
  СКИД не должна ждать эмбеддинги.
- **Удаление:** гарантированное удаление гасит И оригинал в хранилище, И фрагменты в индексе (ТБ-064).
- **Проверка этапа:** в грунтованном режиме чат отвечает по реальному поручению и даёт ссылку на него.

### Этап 8. Тесты — 0.5–1 неделя
- Перенос 116 тестов; **FluentAssertions → Shouldly** (механически).
- Интеграционные — на Testcontainers с настоящим Postgres (InMemory запрещён правилами проекта).
- Новые тесты: фильтр доступа над документами/поручениями (гриф и подразделение соблюдаются);
  документ docflow доходит до индекса и находится retrieval'ом только в пределах допуска.

### Этап 9. Приёмка и деплой — 0.5–1 неделя
**Переноса боевых данных НЕТ** — у СКИД их нет, схема `docflow` поднимается миграциями с нуля.
- Сид демонстрационных данных для приёмки (справочники, типовые документы и поручения).
- Сквозная приёмка: регистрация документа → поручение с исполнителем и сроком → индексация в корпус →
  вопрос в чате, ответ со ссылкой на этот документ, проверка что чужой гриф не выдаётся.
- Деплой одного хоста в контур (с LibreOffice для DOCX→PDF), проверка работы без интернета (air-gap).

## Что нужно решить заказчику (блокирует этапы)

| # | Вопрос | Блокирует |
|---|---|---|
| 1 | ~~Продолжается ли доработка репозитория `skid` параллельно?~~ | ✅ **решено 2026-08-04: доработки не будет**, проект завершён → расхождения кода не возникнет, после переноса репозиторий в архив |
| 2 | Как назначается **гриф** при регистрации документа: выбирает пользователь, наследуется от типа документа или от подразделения? | этапы 6, 7 |
| 3 | Подразделения: один справочник или два? СКИД — плоский Guid; профиль — иерархический `Division` (ТУ→РО), int. **Предложение (реализовано в 2.1 на уровне данных, ждёт подтверждения):** словарь ОДИН — тот, которым живёт решётка доступа ядра (`division_id` int в `core.clearance`/чанках); docflow хранит те же int-идентификаторы слабыми ссылками, СВОЙ справочник НЕ заводит; список подразделений для UI модуль получит через порт, реализацию которого даст профиль (инверсия как у `IAccessPolicy`) — этап 5/6 | этап 6 |
| 4 | ~~Кто владеет **реестром пользователей** после слияния~~ | ✅ **решено 2026-08-05: реестр переезжает в ISC.AI** — приложение СКИД исчезает, держать её БД ради одних учёток бессмысленно (администрировать было бы негде). Подробности — в этапе 6 |

> Вопрос 1 закрыт — **старт не заблокирован**. Вопросы 2–3 нужны к этапу 6 и решаются по ходу.

## Границы и примечания

- **Пересечение модулей.** У ISC.AI уже есть «Дашборд», «Архив», «Мониторинг»; у СКИД — свой дашборд и
  работа с документами. При переносе решается: сливать в один модуль или держать раздельно. Не «само
  собой» — фиксируется решением при этапе 5(UI)/6.
- **API у СКИД нет** (только вход, файловый эндпоинт, SignalR-хаб, Blazor UI) — вариант «интеграция по API»
  физически отсутствует; переносится исходный код.
- Двуязычие СКИД (рус/кырг) — **требование ТЗ (п. 7.1), а не опция**; переносится как есть, в ISC.AI
  аналога пока нет. Влияние на оболочку оценить на этапе 5: либо двуязычие остаётся внутри модуля, либо
  поднимается до общей оболочки — второе шире по объёму.
- Оценка 7–10 недель — **до** калибровочного захода (этап 1). Точная цифра появляется после него.
  История изменений оценки (чтобы срок не «полз» незаметно): 7–10 → **8–11** (добавлен пропущенный этап 7
  «индексация в корпус») → **7–10** (выяснилось, что боевых данных нет — этап переноса данных отпал).
- Из трёх исходных рисков остались два: отчёты (этап 5) и решётка доступа (этап 6). Риск переноса
  боевых данных **снят**.
- Коммиты и мёрдж выполняет заказчик; работы ведутся в ветке `feature/skid-docflow-merge`.
