namespace ISC.AI.Modules.Admin.Application;

/// <summary>
/// Единые тексты отказа сценариев администрирования платформы (ТБ-012).
/// </summary>
/// <remarks>
/// ИНВАРИАНТ: САМО ПРАВИЛО «кто вправе» здесь не живёт и жить не может — его знает только профиль
/// (порт <c>IPlatformAdministration</c>), потому что право администрировать определяется РОЛЬЮ, а
/// модель ролей у каждого эксплуатанта своя. Здесь собраны исключительно формулировки, и собраны
/// затем, чтобы они не разошлись между девятью сценариями: разъехавшиеся тексты отказа замечает не
/// разработчик, а посторонний, который по разнице формулировок узнаёт о системе больше положенного.
/// </remarks>
public static class AdminGuard
{
    /// <summary>Отказ в ведении учётных записей, допусков и справочников платформы.</summary>
    public const string Denied = "Действие доступно только администратору платформы.";

    /// <summary>Отказ в чтении неизменяемого журнала аудита (ТБ-030/032).</summary>
    public const string AuditDenied = "Просмотр журнала аудита доступен только администратору платформы.";

    /// <summary>
    /// Отказ журнала субъекту БЕЗ действующего допуска (fail-closed, ТБ-012/021): право открыть
    /// журнал есть, а режимного контекста для отбора записей нет.
    /// </summary>
    public const string AuditClearanceRequired =
        "Журнал доступен только субъекту с действующим допуском: выдайте себе допуск в разделе «Допуски пользователей».";
}
