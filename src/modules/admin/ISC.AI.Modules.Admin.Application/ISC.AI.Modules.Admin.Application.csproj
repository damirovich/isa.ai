<Project Sdk="Microsoft.NET.Sdk">

  <!-- Сценарии пакета «Администрирование платформы»: учётные записи, допуски, журнал аудита, смена
       своего пароля. Данные — через порты ЯДРА (IUserAccountStore, IClearanceStore, IAuditReader),
       поэтому слоя данных у пакета нет; право администрировать — через порт профиля. -->
  <PropertyGroup>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\..\core\ISC.AI.Abstractions\ISC.AI.Abstractions.csproj" />
    <ProjectReference Include="..\ISC.AI.Modules.Admin.Domain\ISC.AI.Modules.Admin.Domain.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Mediator.Abstractions" />
    <PackageReference Include="FluentValidation" />
    <PackageReference Include="FluentValidation.DependencyInjectionExtensions" />
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" />
    <!-- Стартовая сверка допусков со справочником подразделений — BackgroundService. -->
    <PackageReference Include="Microsoft.Extensions.Hosting.Abstractions" />
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
  </ItemGroup>

</Project>
