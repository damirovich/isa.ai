<Project Sdk="Microsoft.NET.Sdk">

  <!-- Доменные сущности документооборота (документ, поручение, срок, статус, комментарий) — переносятся
       из СКИД на этапе 2 (Э4-35). Зависит только от Abstractions: модуль переиспользуем ЛЮБЫМ профилем. -->
  <ItemGroup>
    <ProjectReference Include="..\..\..\core\ISC.AI.Abstractions\ISC.AI.Abstractions.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
